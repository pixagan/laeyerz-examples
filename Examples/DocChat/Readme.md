## This is a Document Chat Example
It consists of two workflows, one that takes in the document and stores it. 

The second is the Retrieval flow that given the user query, retrieves the relevant information from the vector store and then calls the llm to get the response.